01. SQL 응용

1. 절차형 SQL 작성(3-2)
    (1) 절차형 SQL
        개념
        종류
    (2) SQL 문법
        개념
        분류(정조제)
        WHERE 조건
        LIKE와 같이 사용하는 와일드 문자

2. 응용 SQL 작성(3-7)
    (1) 데이터 조작어(DML)
        개념
        유형(세인업데)
        명령어 : SELECT(셀프 웨 구해오) / INSERT / UPDATE(업셋웨) / DELETE(델프웨)
    (2) 데이터 제어어(DCL)
        개념
        기능(보무병회)
        유형
        명령어 : GRANT 명령어(그온투) / REVOKE(권한취소)명령어(리온프) / TCL명령어
    (3) 윈도 함수
        개념
        구문
        분류(집 순행비)
        활용
